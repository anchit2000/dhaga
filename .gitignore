# local Claude Code overrides (personal, not team)
.claude/settings.local.json

# dependencies & builds (for when apps/ exist)
node_modules/
.next/
dist/
build/
.expo/

# transformers.js model cache
apps/web/node_modules/.cache/

# env & secrets
.env
.env.*
!.env.example

# local embedded database (PGlite)
.dhaga-data/

# OS noise
.DS_Store
Thumbs.db
