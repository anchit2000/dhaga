{
  "name": "@dhaga/extension",
  "version": "1.0.0",
  "private": true,
  "description": "Dhaga browser extension: one-click capture from any page into your network.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "package": "node build.mjs --zip",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "esbuild": "^0.25.12",
    "typescript": "^5"
  }
}
