---
title: "isOverloaded()"
---

```ts
function isOverloaded(params): boolean;
```

Defined in: calendar/availability.ts:121

True when the local day already holds `maxMeetingsPerDay` or more meetings.

## Parameters

### params

#### day

`Date`

#### busy

[`BusyInterval`](../interfaces/BusyInterval)[]

#### maxMeetingsPerDay

`number`

#### utcOffsetMinutes?

`number`

## Returns

`boolean`
