---
title: "methodValues()"
---

```ts
function methodValues(raw): string[];
```

Defined in: schemas/contact-fields.ts:102

The bare values only — for search indexing, export columns, mailto, etc.

## Parameters

### raw

`unknown`

## Returns

`string`[]
