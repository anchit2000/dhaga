---
title: "BatchExtractItem\\<T\\>"
---

Defined in: llm/types.ts:53

## Extends

- `Omit`\<[`ExtractOptions`](ExtractOptions)\<`T`\>, `"images"`\>

## Type Parameters

### T

`T`

## Properties

### schema

```ts
schema: ZodType<T>;
```

Defined in: llm/types.ts:28

#### Inherited from

```ts
Omit.schema
```

***

### system

```ts
system: string;
```

Defined in: llm/types.ts:29

#### Inherited from

```ts
Omit.system
```

***

### prompt

```ts
prompt: string;
```

Defined in: llm/types.ts:30

#### Inherited from

```ts
Omit.prompt
```

***

### tier

```ts
tier: ModelTier;
```

Defined in: llm/types.ts:31

#### Inherited from

```ts
Omit.tier
```

***

### maxTokens?

```ts
optional maxTokens?: number;
```

Defined in: llm/types.ts:32

#### Inherited from

```ts
Omit.maxTokens
```

***

### id

```ts
id: string;
```

Defined in: llm/types.ts:59

Caller-chosen id, echoed back on the matching result — how a caller maps
a result back to whatever it was for (e.g. a contact id). Becomes the
batch request's `custom_id`; must match `^[a-zA-Z0-9_-]{1,64}$`.
