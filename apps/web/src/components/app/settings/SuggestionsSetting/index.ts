export { SuggestionsSetting } from "./SuggestionsSetting";
// Shared with the sibling ImportantDatesSetting card — one email-preference
// switch, one `action` prop per preference.
export { DigestToggle } from "./DigestToggle";
