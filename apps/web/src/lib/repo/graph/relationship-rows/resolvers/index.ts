// Split per the 150-line rule; the "./resolvers" import path is unchanged.
export { resolveSubject, subjectOptions, type SubjectResolution } from "./subject";
export { resolveObject, type ObjectResolution } from "./object";
