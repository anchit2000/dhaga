/**
 * The product-stage marker shown beside the Dhaga wordmark, in the marketing
 * header and in the signed-in app shell. One string, one place: the day Dhaga
 * leaves beta, this file is the only edit — and both surfaces stop saying it
 * together, rather than one going stale behind the other.
 */

/** Visible pill text. Rendered as-is; the pill uppercases it in CSS. */
export const BETA_LABEL = "Beta";

/** Hover/description text — what "beta" actually means for the reader. */
export const BETA_NOTE = "Dhaga is in beta — features are still landing.";
