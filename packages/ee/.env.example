# Dhaga Cloud only — see LICENSE. Self-hosted instances don't need any of
# this; leave DHAGA_HOSTED_MODE unset and none of it is ever read.

# Turns on early-access gating, the admin panel, and Stripe billing. All
# three read/write real Postgres (RLS) — DATABASE_URL (in apps/web/.env)
# must point at a real Postgres instance, not the embedded PGlite default.
DHAGA_HOSTED_MODE=true

# Bootstrap admin escape hatch — comma-separated emails treated as admin
# before anyone has flipped the isAdmin flag through the (not yet built)
# UI for it.
DHAGA_ADMIN_EMAILS=

# Stripe (test mode keys while developing: https://dashboard.stripe.com/test/apikeys)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Price IDs from the Stripe Dashboard — Products. One per (tier, cadence);
# leave a pair blank and that combination simply isn't offered in the UI.
# Reference prices (BRD 8.3): Pro $10/mo or $96/yr, Power $30/mo or $288/yr.
# STRIPE_PRICE_PRO_ANNUAL keeps its historical name — do NOT rename to _YEARLY,
# live instances already set it.
STRIPE_PRICE_PRO_MONTHLY=
STRIPE_PRICE_PRO_ANNUAL=
STRIPE_PRICE_POWER_MONTHLY=
STRIPE_PRICE_POWER_ANNUAL=
# Multi-currency (a UAE visitor seeing dirhams) is Stripe ADAPTIVE PRICING —
# a Dashboard setting on Checkout, not code. Enable it there; do not add
# per-currency price env vars.

# Razorpay (INR) — optional, independent of Stripe. Setting BOTH keys turns on
# an "Pay in INR" button in /app/settings alongside (or instead of) Stripe's.
# Test keys: https://dashboard.razorpay.com/app/website-app-settings/api-keys
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
# No NEXT_PUBLIC_ mirror is needed: /api/razorpay/order returns the publishable
# key id with the checkout handoff, so the browser gets it from the same source
# of truth the object was created with and the two can never drift apart.
#
# Per-endpoint secret from Dashboard > Settings > Webhooks. This is NOT the API
# key secret above — using that one rejects every event. Point the endpoint at
# https://your-domain/api/razorpay/webhook and subscribe to at least
# subscription.activated / .charged / .halted / .cancelled and payment.captured.
RAZORPAY_WEBHOOK_SECRET=
# Razorpay PLAN ids (plan_...), not amounts: the plan owns both the price and
# the billing interval. One per (tier, cadence); blank = not offered.
# Reference prices: Pro ₹899/mo or ₹8,499/yr, Power ₹2,599/mo or ₹24,999/yr
# (≈ parity with the USD figures at ~₹87/USD, so BRD 8.3's margins hold).
RAZORPAY_PLAN_PRO_MONTHLY=
RAZORPAY_PLAN_PRO_YEARLY=
RAZORPAY_PLAN_POWER_MONTHLY=
RAZORPAY_PLAN_POWER_YEARLY=

# Founding Pro: ₹6,999 for the FIRST year against the standard ₹8,499, capped
# at the first 500 seats. Blank = the offer does not exist anywhere in the UI.
# Razorpay only, deliberately — there is no Stripe price, because a USD
# checkout would mint a seat the cap can't see. The cap is a constant
# (billing/founding/cap.ts), not an env var: it is a promise printed on a
# public page. Seats are claimed when checkout is created, so an abandoned
# checkout holds one — the offer can sell fewer than 500, never more.
RAZORPAY_PLAN_PRO_FOUNDING_YEARLY=

# Connection-pool tuning (read from the same apps/web env core reads — this
# file is just a documentation copy). The EE tenant pool draws from Supabase's
# fixed session pool_size of 15 shared across all warm instances; see
# apps/web/.env.example and docs/SCALING.md §2 for the full 15-slot math.
# DB_POOL_MAX_TENANT=3           # EE tenant pool max (core pool: DB_POOL_MAX_CORE, default 2)
# When the pooler transiently rejects a new backend (EMAXCONNSESSION / "max
# clients reached" / connect timeout), acquisition retries with backoff+jitter
# (packages/ee/src/db/connect-retry.ts). Raising Supabase's pool_size is the
# durable capacity lever. Tune the retry (shared with the core pool):
# DB_CONNECT_RETRY_MAX=5         # max acquisition attempts incl. the first
# DB_CONNECT_RETRY_BASE_MS=100   # first backoff step in ms (doubles each retry)
